#pragma once

// Copy this file to wifi_secrets.h and fill in your real values.
// Keep wifi_secrets.h out of version control (.gitignore already handles it).

const char* WIFI_SSID        = "YOUR_WIFI_NAME";
const char* WIFI_PASSWORD    = "YOUR_WIFI_PASSWORD";
const char* ALERT_SHARED_KEY = "CHANGE_ME_SECRET";

